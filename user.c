//won't let me pull/push race.c to add these changes in

void *carUser(void *aString) {
  char* str;
  char spaces;
  str = (char*) aString;
  printf("%s\n", str);
  spaces = scanf("%c", value); //error on value
  while (user < 40) { //says expected an ")"
      if scanf() == "" {
          user++;
      } else {
          sleep(0); //dont move the car
      }
   }
   pthread_exit(NULL);
} //end carUser 
